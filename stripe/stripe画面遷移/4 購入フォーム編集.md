
---

Blade のボタン先を stripe.checkout.create に

```html
<form method="POST" action="{{ route('stripe.checkout.create', $item) }}">
  @csrf
  {{-- 画面で「カード/コンビニ」を選ばせたいなら保持用のradio（任意） --}}
  <label><input type="radio" name="pm" value="card" checked>カード</label>
  <label><input type="radio" name="pm" value="konbini">コンビニ</label>
  <button type="submit">購入する</button>
</form>
```
