
---

#### route設定

```php
// ルート（routes/web.php）

Route::get('/checkout/success', [CheckoutController::class, 'show'])
    ->name('checkout.success');
Route::post('/stripe/webhook', [StripeWebhookController::class, 'handle'])
    ->name('stripe.webhook');
```